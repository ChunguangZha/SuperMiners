<Window x:Class="SuperMinersServerApplication.Views.DeleteAllStoneSellOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DeleteAllStoneSellOrderWindow" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <DataGrid Name="datagrid" AutoGenerateColumns="True" IsReadOnly="True">
            
        </DataGrid>
        <Button Grid.Row="1" Name="btnClear" Content="开始处理" Margin="10" Width="100" Click="btnClear_Click"/>
    </Grid>
</Window>
